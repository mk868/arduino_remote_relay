#ifndef FIRMWARE_COMMON_H
#define FIRMWARE_COMMON_H

#include <string.h>

/**
 * Returns true when specified string starts with specified prefix.
 */
bool str_prefix(const char *str, const char *prefix);

#endif //FIRMWARE_COMMON_H
